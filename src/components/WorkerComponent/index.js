import React from "react";
import { WorkerContainer } from "containers";

import "./WorkerComponent.scss";

const WorkerComponent = () => (
  <div className="workerComponent">
  	<WorkerContainer/>
  </div>
);

export default WorkerComponent;
